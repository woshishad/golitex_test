claim:
    prove:
        forall a, b R:
            a < b or a = b or a > b

claim:
    prove:
        forall a, b R:
            a <= b
            =>:
                a = b or a < b

import Int

claim:
    prove:
        forall x R:
            Int::floor(x) $in R
    Int::floor(x) $in R
